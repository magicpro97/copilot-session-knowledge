{
  "_meta": {
    "id": "healthz",
    "endpoint": "GET /healthz",
    "description": "Health-check endpoint — liveness-only payload (#560). No DB reads; no activity counts; no timestamps."
  },
  "status": 200,
  "content_type_prefix": "application/json",
  "body_shape": {
    "status": "ok",
    "sync_status_endpoint": "/api/sync/status"
  }
}
