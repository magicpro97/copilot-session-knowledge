{
  "skill_name": "hook-creator",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a hook that blocks commits containing TODO comments in production code",
      "expected_output": "A preToolUse .py script that reads toolName/toolArgs from stdin JSON, checks for TODO patterns, and outputs deny JSON when found",
      "expectations": [
        "Creates a .py file with #!/usr/bin/env python3 shebang",
        "Reads JSON from stdin using stdlib json",
        "Outputs valid deny JSON with permissionDecision and permissionDecisionReason",
        "Handles edge cases (empty input, non-edit tools)",
        "Uses commit-gate.py as starting template"
      ]
    },
    {
      "id": 2,
      "prompt": "Create a hook that prevents API keys from being committed to the repository",
      "expected_output": "A preToolUse script based on secret-detector.py template with API key regex patterns",
      "expectations": [
        "Reads secret-detector.py from references/ as starting template",
        "Includes common API key patterns (AWS, GCP, GitHub, etc.)",
        "Excludes placeholder values (example, TODO, REPLACE_ME)",
        "Tests against sample content to verify detection"
      ]
    },
    {
      "id": 3,
      "prompt": "Set up coding standards enforcement for a Python Django project that should not use print(), wildcard imports, or bare except",
      "expected_output": "A customized enforce-coding-standards.py with Python rules and ruff integration",
      "expectations": [
        "Reads enforce-coding-standards.py from references/ as starting template",
        "Sets FILE_EXTENSIONS to 'py'",
        "Adds regex rules for print(), 'from x import *', and bare except",
        "Uncomments ruff linter integration (Option C)",
        "Removes or comments out JavaScript-specific rules"
      ]
    },
    {
      "id": 4,
      "prompt": "Create a TDD pipeline enforcement hook with 3 phases: tests, review, and final verification",
      "expected_output": "A customized enforce-tdd-pipeline.py with 3-phase configuration",
      "expectations": [
        "Reads enforce-tdd-pipeline.py from references/ as starting template",
        "Configures PHASES array with 3 phases instead of 5",
        "Keeps content validation, freshness check, and git SHA linking",
        "Removes unused validator functions for phases not in the pipeline"
      ]
    }
  ]
}
