# Neon Postgres connection string for gateway storage
SYNC_GATEWAY_DATABASE_URL=postgresql://<user>:<password>@<host>/<db>?sslmode=require

# Railway injects PORT automatically; default is 8787 for local runs
PORT=8787
HOST=0.0.0.0
